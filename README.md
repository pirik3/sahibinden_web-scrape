[->] Use Proxy or VPN to bypass as detecting for bot.
[->] Sometimes CloudFlare is not bypasses.
[** -> **] Listing only can be scanned for 20 pages as if 50 listing in per page, and this means around 1000 listing max can be scanned out of all others. To solve this issue, we can divide each scans in filter, for example; city to city or even more detailed with town as well. This will give us less than 1k listing and we have to loop around filters.
[] Browser extension to see price changes to be added "classified-price-container".
